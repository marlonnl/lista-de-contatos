import { BootstrapIcon } from '../../styles'
import { AddButtonStyle } from './styles'

const AddButton = () => (
  <AddButtonStyle to="novo">
    <BootstrapIcon>&#xF89A;</BootstrapIcon>
    novo contato
  </AddButtonStyle>
)

export default AddButton
